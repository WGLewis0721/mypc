---
import Icon from "./Icon.astro";
/**
 * Decorative image stand-in. Keeps the aspect ratio reserved so swapping in a real
 * <img> causes no layout shift.
 */
interface Props {
  ratio?: string; // e.g. "4 / 5", "4 / 3", "16 / 9"
  label?: string;
  tone?: "navy" | "navy-800";
  class?: string;
}
const { ratio = "4 / 3", label, tone = "navy-800", class: cls } = Astro.props;
const bg = tone === "navy" ? "var(--color-navy-900)" : "var(--color-navy-800)";
---
<div class={`ph ${cls ?? ""}`} style={`--ph-ratio:${ratio};--ph-bg:${bg}`} aria-hidden="true">
  <Icon name="star" size={22} />
  {label && <span class="ph__label">{label}</span>}
</div>

<style>
  .ph {
    aspect-ratio: var(--ph-ratio);
    width: 100%;
    background: var(--ph-bg);
    border: 1px solid var(--border-on-navy);
    border-radius: var(--radius-md);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 1rem;
    text-align: center;
    color: var(--color-gold-500);
  }
  .ph__label {
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-cloud);
    max-width: 26ch;
    text-wrap: balance;
  }
</style>
